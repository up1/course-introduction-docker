version: "2"

services:
    myweb:
        image: nginx:latest
        ports:
            - "80:80"
        volumes:
            - ./site.conf:/etc/nginx/conf.d/default.conf
        volumes_from:
            - myphp
    myphp:
        image: php:fpm
        volumes:
            - ./web:/web
